        <label for="{{itemId}}" class="{{fieldType}}-inline">
            <input id="{{itemId}}" class="{{requiredClass}}" name="{{fieldName}}" type="{{fieldType}}" value="{{optionValue}}"{{requiredField}}{{checkedItem}}>
            {{itemTitle}}
        </label>