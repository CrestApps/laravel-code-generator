        <select class="form-control{{cssClass}}" id="{{fieldName}}" name="{{fieldName}}" {{fieldMultiple}}{{requiredField}}>
        	    {{placeHolder}}
        	@foreach ({{fieldItems}} as $key => {{fieldItem}})
			    <option value="{{ {{fieldValueAccessor}} }}"{{selectedValue}}>
			    	{{ {{fieldItemAccessor}} }}
			    </option>
			@endforeach
        </select>
        